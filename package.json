{
  "name": "@comparto/package-name",
  "version": "0.0.0",
  "main": "src/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/comparto/package-name.git"
  },
  "author": "@jimmy-guzman <jimmy.guzman@outlook.com>",
  "license": "MIT",
  "keywords": [],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "prettify": "prettier '**/**/*.{js,md,yml}' --write",
    "lint": "eslint --cache --cache-location ./node_modules/.cache/eslint/ . --ext .js",
    "lint:fix": "yarn lint --fix",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:ci": "yarn test:coverage",
    "cz": "npx gitzy --commitlint"
  },
  "prettier": "@comparto/prettier-config",
  "husky": {
    "hooks": {
      "commit-msg": "[[ -n $HUSKY_BYPASS ]] || commitlint -E HUSKY_GIT_PARAMS",
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{md,yml}": [
      "prettier --write"
    ],
    "*.{js}": [
      "eslint --fix"
    ]
  },
  "peerDependencies": {},
  "dependencies": {},
  "devDependencies": {
    "@comparto/eslint-config": "6.1.0",
    "@comparto/prettier-config": "^1.1.0",
    "@semantic-release/changelog": "^5.0.1",
    "@semantic-release/git": "^9.0.0",
    "commitlint": "^11.0.0",
    "eslint": "^7.14.0",
    "gitzy": "^1.3.1",
    "husky": "^4.3.7",
    "jest": "^26.6.3",
    "lint-staged": "^10.5.3",
    "prettier": "^2.0.5",
    "semantic-release": "^17.3.1",
    "typescript": "^4.0.0"
  },
  "files": [
    "src"
  ]
}
